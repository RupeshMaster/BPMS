# Bharat Petroleum Assets Directory

Please place your original prototype images in this directory with the following exact filenames:

1. `logo.png` (BP logo, circle graphic, 60x60px / 94x88px)
2. `hero.png` (Landing page hero background image, large resolution)
3. `login-bg.png` (Login page split layout left-side background image)

The CSS and layouts in this React project are already configured to load these files with proper alignment.
